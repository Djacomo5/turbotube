{
  "name": "turbotube-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend",
    "packages/shared"
  ],
  "scripts": {
    "dev:frontend": "npm run dev -w apps/frontend",
    "dev:backend": "npm run dev -w apps/backend",
    "dev": "concurrently --kill-others \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build:shared": "npm run build -w packages/shared",
    "build:backend": "npm run build:shared && npm run build -w apps/backend",
    "build:frontend": "npm run build:shared && npm run build -w apps/frontend",
    "build": "npm run build:shared && npm run build:backend && npm run build:frontend",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
